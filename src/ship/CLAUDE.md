# ship/

Ship entities and movement.

## Files

- `mod.rs` — Ship marker, ShipStats (speed, vision_range), MovementTarget, Selected marker, SelectionIndicator marker, movement system (move toward target, remove on arrival), bounds clamping, spawn_ship helper
