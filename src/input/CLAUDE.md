# input/

Player input handling via Bevy picking observers.

## Files

- `mod.rs` — on_ship_clicked (left-click observer for ship selection), on_ground_clicked (right-click observer for move commands), selection indicator torus that follows selected ship, Escape to deselect
