# camera/

Free camera controller for top-down tactical view.

## Files

- `mod.rs` — GameCamera marker, CameraSettings resource, camera spawn (with directional + ambient light), pan (WASD/arrows), zoom (scroll wheel), rotate (middle-mouse orbit around ground intersection)
